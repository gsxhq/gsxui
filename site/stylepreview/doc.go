// Package stylepreview holds the theme editor's preview compositions. The
// authored gallery source (gallery.gsx.src) lives here; internal/stylegen
// rewrites its package clause into every style's preview package (nova/,
// maia/), which are generated and must never be edited by hand.
package stylepreview
