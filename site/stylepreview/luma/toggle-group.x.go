// Code generated by gsx; DO NOT EDIT.

package luma

import (
	_gsxctx "context"
	"github.com/gsxhq/gsx"
	_gsxrt "github.com/gsxhq/gsx"
	_gsxstd "github.com/gsxhq/gsx/std"
	_gsxcm "github.com/gsxhq/gsxui/merge"
	_gsxio "io"
	_gsxsc "strconv"
)

//line toggle-group.gsx:5:1
// ToggleGroup and ToggleGroupItem are the shadcn/ui ToggleGroup
// (registry/new-york-v4/ui/toggle-group.tsx) plus the runtime attributes
// Radix's @radix-ui/react-toggle-group / react-roving-focus / react-toggle
// stamp at mount (traced from their dist/index.mjs, not guessed from the
// .tsx — see 2026-07-24 controls source map, `## toggle-group`).
//
// GAP (no context, group->item is explicit params): Radix's
// ToggleGroupContext.Provider broadcasts type/variant/size/spacing from
// ToggleGroup down to every ToggleGroupItem automatically; gsx has no
// context, so the caller — which already has all four values in scope when
// building the tree — passes them explicitly to ToggleGroup AND to every
// ToggleGroupItem, same shape as `## tabs`' selected. Radix's group-level
// `disabled` OR-cascading onto every item (context.disabled ||
// props.disabled) is the same story: no param for it here, disabled flows
// through each element's own attrs bag (native <button disabled> on the
// item, an inert-but-present attribute on the root's <div>) — the caller
// disables the whole group by passing disabled to every item explicitly.
//
// ADAPT (groupType, not type): shadcn's own prop is named `type` — a Go
// keyword, unusable as a component parameter name (unlike `select`/`switch`,
// which only needed a Go-keyword workaround at the file/package level, see
// select.gsx's own doc comment). `groupType == "single"` selects
// role="radiogroup" (root) / role="radio" aria-checked (item); anything else
// (including the Go zero value "") renders role="toolbar" (root) /
// aria-pressed, no role override (item) — the "multiple" behavior. Radix
// itself throws if `type` is omitted; Go can't throw at render-construction
// time the way React throws at mount, so this is a doc-comment API-design
// note (pass groupType explicitly), not a runtime check.
//
// ADAPT (root shadow selector dropped, not ported dead): new-york-v4's root
// class carries `data-[spacing=default]:data-[variant=outline]:shadow-xs`,
// but `data-spacing` is stamped as the literal (numeric) prop value, never
// the string "default" — the selector can never match anything the
// component itself renders, dead CSS in the shadcn source itself (see
// docs/jsx-parity.md FINDING for the full trace). nova's own
// .cn-toggle-group already drops it rather than porting it as dead weight;
// this port follows nova's own precedent instead of the house's usual
// "port dead weight, ledger it" convention — see docs/jsx-parity.md.
//
// ADAPT (horizontal-only v1): data-orientation="horizontal" is stamped on
// both root and item, but only the horizontal corner-rounding selectors are
// ported (data-[orientation=horizontal]:data-[spacing=0]:first:rounded-l-lg
// / last:rounded-r-lg) — vertical is real new functionality nova adds
// (Radix's own new-york-v4 markup never varies rounding by orientation) and
// is out of v1 scope; see docs/jsx-parity.md GAP. toggle-group.js gates its
// arrow-key handling the same way (ArrowLeft/Up and ArrowRight/Down both
// move focus, since orientation is always "horizontal" here).
//
// MECHANISM (roving tabindex, JS-normalized at init): server renders every
// item with no tabindex attribute at all — a graceful no-JS fallback where
// every item is its own tab stop. toggle-group.js sets tabindex="0" on
// exactly one item at init, then maintains that invariant on every arrow
// move and click — see its own header comment. Entry-tabstop priority is
// type-agnostic (matching Radix's own traced RovingFocusGroup: the "active"
// candidate wins regardless of type, else fall back to the first item —
// the map's behavior contract never conditions this on type="single" vs
// "multiple"): the pressed-and-enabled item if one exists, else the first
// non-disabled item — the same rule applies whether groupType is "single"
// or "multiple".

//line toggle-group.gsx:64:1
func ToggleGroup(groupType string, variant string, size string, spacing string, children gsx.Node, attrs gsx.Attrs) _gsxrt.Node {
	return _gsxrt.Func(func(ctx _gsxctx.Context, _gsxw _gsxio.Writer) error {
		_gsxgw := _gsxrt.W(_gsxw)
		return _gsxrenderToggleGroup(ctx, _gsxgw, groupType, variant, size, spacing, children, attrs)
	})
}

func _gsxrenderToggleGroup(ctx _gsxctx.Context, _gsxgw *_gsxrt.Writer, groupType string, variant string, size string, spacing string, children gsx.Node, attrs gsx.Attrs) error {
	if _gsxerr := _gsxgw.Err(); _gsxerr != nil {
		return _gsxerr
	}
//line toggle-group.gsx:65:2
	sp := spacing
	if sp == "" {
		sp = "0"
	}
	role := "toolbar"
	if groupType == "single" {
		role = "radiogroup"
	}
//line toggle-group.gsx:75:2
	_gsxgw.S("<div")
	if !attrs.Has("data-variant") {
		_gsxgw.S(" data-variant=\"")
		_gsxgw.AttrValue(string(_gsxstd.Default((variant), "default")))
		_gsxgw.S("\"")
	}
	if !attrs.Has("data-size") {
		_gsxgw.S(" data-size=\"")
		_gsxgw.AttrValue(string(_gsxstd.Default((size), "default")))
		_gsxgw.S("\"")
	}
	if !attrs.Has("data-spacing") {
		_gsxgw.S(" data-spacing=\"")
		_gsxgw.AttrValue(string(sp))
		_gsxgw.S("\"")
	}
	if !attrs.Has("data-orientation") {
		_gsxgw.S(" data-orientation=\"horizontal\"")
	}
	if !attrs.Has("role") {
		_gsxgw.S(" role=\"")
		_gsxgw.AttrValue(string(role))
		_gsxgw.S("\"")
	}
	if !attrs.Has("style") {
		_gsxgw.S(" style=\"--gap: ")
		_gsxgw.AttrValue(_gsxrt.StyleValue(string(sp)))
		_gsxgw.S("\"")
	}
	_gsxv0 := "group/toggle-group"
	_gsxv1 := "data-[spacing=0]:data-[variant=outline]:rounded-3xl flex"
	var _gsxv2 string
	switch size {
	case "sm":
		_gsxv2 = "rounded-[min(var(--radius-md),10px)]"
	case "lg":
		_gsxv2 = "rounded-lg"
	default:
		_gsxv2 = "rounded-lg"
	}
	_gsxgw.S(" class=\"")
	_gsxgw.Class(_gsxcm.Merge, _gsxrt.Class(_gsxv0), _gsxrt.Class(_gsxv1), _gsxrt.Class(_gsxv2), _gsxrt.Class(attrs.Class()))
	_gsxgw.S("\"")
	_gsxgw.StyleMerged("", attrs.Style())
	_gsxgw.Spread(ctx, "div", attrs, _gsxrt.AttrSinks{}, []string{"class", "style", "data-gsxui-slot-toggle-group"})
	_gsxgw.BoolAttr("data-gsxui-slot-toggle-group", true)
	_gsxgw.S(">")
//line toggle-group.gsx:90:3
	_gsxgw.Node(ctx, children)
	_gsxgw.S("</div>")
	return _gsxgw.Err()
}

//line toggle-group.gsx:94:1
// ToggleGroupItem composes ordered tokens "toggle toggle-group-item".
// Variant, size, spacing, orientation, and state are public CSS axes; only
// the ARIA attribute pair differs between single and multiple groups.
//
// MECHANISM (single-type replace-on-activate): clicking a new item in a
// type="single" group simply sets a new single value — there is no
// group-level "uncheck the others" loop needed here because ToggleGroup
// itself carries no value state to update; toggle-group.js re-stamps
// data-state="off"/aria-checked="false" on every OTHER item sharing the
// same root when a type="single" item activates, so exactly one item shows
// data-state="on" at a time (Radix's onItemActivate === setValue, restated
// for a stateless server render). Clicking the already-pressed item in a
// single group toggles it off (Radix allows an empty single value unless a
// caller opts otherwise) — port the same replace-on-activate mechanic.

//line toggle-group.gsx:108:1
func ToggleGroupItem(groupType string, variant string, size string, spacing string, pressed bool, value string, children gsx.Node, attrs gsx.Attrs) _gsxrt.Node {
	return _gsxrt.Func(func(ctx _gsxctx.Context, _gsxw _gsxio.Writer) error {
		_gsxgw := _gsxrt.W(_gsxw)
		return _gsxrenderToggleGroupItem(ctx, _gsxgw, groupType, variant, size, spacing, pressed, value, children, attrs)
	})
}

func _gsxrenderToggleGroupItem(ctx _gsxctx.Context, _gsxgw *_gsxrt.Writer, groupType string, variant string, size string, spacing string, pressed bool, value string, children gsx.Node, attrs gsx.Attrs) error {
	if _gsxerr := _gsxgw.Err(); _gsxerr != nil {
		return _gsxerr
	}
//line toggle-group.gsx:109:2
	sp := spacing
	if sp == "" {
		sp = "0"
	}
	state := "off"
	if pressed {
		state = "on"
	}
	orientation := "horizontal"
//line toggle-group.gsx:120:2
	_gsxgw.S("<button")
	if !attrs.Has("type") {
		_gsxgw.S(" type=\"button\"")
	}
	if !attrs.Has("data-variant") {
		_gsxgw.S(" data-variant=\"")
		_gsxgw.AttrValue(string(_gsxstd.Default((variant), "default")))
		_gsxgw.S("\"")
	}
	if !attrs.Has("data-size") {
		_gsxgw.S(" data-size=\"")
		_gsxgw.AttrValue(string(_gsxstd.Default((size), "default")))
		_gsxgw.S("\"")
	}
	if !attrs.Has("data-spacing") {
		_gsxgw.S(" data-spacing=\"")
		_gsxgw.AttrValue(string(sp))
		_gsxgw.S("\"")
	}
	if !attrs.Has("data-orientation") {
		_gsxgw.S(" data-orientation=\"")
		_gsxgw.AttrValue(string(orientation))
		_gsxgw.S("\"")
	}
	if !attrs.Has("data-state") {
		_gsxgw.S(" data-state=\"")
		_gsxgw.AttrValue(string(state))
		_gsxgw.S("\"")
	}
	if !attrs.Has("data-value") {
		_gsxgw.S(" data-value=\"")
		_gsxgw.AttrValue(string(value))
		_gsxgw.S("\"")
	}
	if groupType == "single" {
		if !attrs.Has("role") {
			_gsxgw.S(" role=\"radio\"")
		}
		if !attrs.Has("aria-checked") {
			_gsxgw.S(" aria-checked=\"")
			_gsxgw.S(_gsxsc.FormatBool(bool(pressed)))
			_gsxgw.S("\"")
		}
	} else {
		if !attrs.Has("aria-pressed") {
			_gsxgw.S(" aria-pressed=\"")
			_gsxgw.S(_gsxsc.FormatBool(bool(pressed)))
			_gsxgw.S("\"")
		}
	}
	_gsxv3 := "hover:text-foreground focus-visible:border-ring focus-visible:ring-ring/30 aria-invalid:ring-destructive/20 dark:aria-invalid:ring-destructive/40 aria-invalid:border-destructive disabled:pointer-events-none disabled:opacity-50 data-[state=on]:bg-muted data-[variant=outline]:border-input data-[variant=outline]:hover:bg-muted data-[variant=outline]:border gap-1 rounded-3xl text-sm font-medium transition-colors [&_svg:not([class*='size-'])]:size-4 group-data-[spacing=0]/toggle-group:rounded-none group-data-[spacing=0]/toggle-group:px-3 group-data-[spacing=0]/toggle-group:shadow-none group-data-horizontal/toggle-group:data-[spacing=0]:first:rounded-l-3xl group-data-vertical/toggle-group:data-[spacing=0]:first:rounded-t-3xl group-data-horizontal/toggle-group:data-[spacing=0]:last:rounded-r-3xl group-data-vertical/toggle-group:data-[spacing=0]:last:rounded-b-3xl"
	var _gsxv4 string
	switch size {
	case "sm":
		_gsxv4 = "h-8 min-w-8 px-3 has-[>svg]:px-2 group-data-[spacing=0]/toggle-group:has-[>svg]:px-2"
	case "lg":
		_gsxv4 = "h-10 min-w-10 px-4 has-[>svg]:px-3 group-data-[spacing=0]/toggle-group:has-[>svg]:px-3"
	default:
		_gsxv4 = "h-9 min-w-9 px-3 has-[>svg]:px-2.5 group-data-[spacing=0]/toggle-group:has-[>svg]:px-2.5"
	}
	var _gsxv5 string
	switch sp {
	case "2":
		_gsxv5 = "isolate"
	default:
		_gsxv5 = "rounded-none shadow-none data-[variant=outline]:border-s-0 data-[variant=outline]:first:border-s"
	}
	var _gsxv6 string
	switch orientation {
	default:
		_gsxv6 = "data-[spacing=0]:first:rounded-s-lg data-[spacing=0]:last:rounded-e-lg"
	}
	_gsxgw.S(" class=\"")
	_gsxgw.Class(_gsxcm.Merge, _gsxrt.Class(_gsxv3), _gsxrt.Class(_gsxv4), _gsxrt.Class(_gsxv5), _gsxrt.Class(_gsxv6), _gsxrt.Class(attrs.Class()))
	_gsxgw.S("\"")
	_gsxgw.StyleMerged("", attrs.Style())
	_gsxgw.Spread(ctx, "button", attrs, _gsxrt.AttrSinks{}, []string{"class", "style", "data-gsxui-slot-toggle-group-item", "data-gsxui-slot-toggle"})
	_gsxgw.BoolAttr("data-gsxui-slot-toggle-group-item", true)
	_gsxgw.BoolAttr("data-gsxui-slot-toggle", true)
	_gsxgw.S(">")
//line toggle-group.gsx:156:3
	_gsxgw.Node(ctx, children)
	_gsxgw.S("</button>")
	return _gsxgw.Err()
}
