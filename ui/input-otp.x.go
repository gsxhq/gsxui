// Code generated by gsx; DO NOT EDIT.

package ui

import (
	_gsxctx "context"
	"github.com/gsxhq/gsx"
	_gsxrt "github.com/gsxhq/gsx"
	_gsxcm "github.com/gsxhq/gsxui/merge"
	"github.com/gsxhq/gsxui/ui/icon"
	_gsxio "io"
)

//line input-otp.gsx:8:1
// InputOTP and its parts are the shadcn/ui InputOTP (registry/new-york-v4/
// ui/input-otp.tsx), a thin wrapper over the `input-otp` npm library. The
// library's real architecture (traced from its own ~200-line public
// contract, not guessable from shadcn's .tsx alone — see the 2026-07-24
// controls source map, `## input-otp`) is the entire mechanism this port
// carries over:
//
// MECHANISM (ONE real input, not N): there is exactly ONE actual
// `<input>` element — data-gsxui-input-otp-input below, absolutely
// positioned to cover the whole slots row, visually hidden with opacity-0
// (never sr-only/hidden/display:none, which would break focusability) and
// left clickable (no pointer-events-none, kept z-10 on top) so a native
// click already moves the caret even before any JS runs. It owns focus,
// caret, and paste. The InputOTPSlot elements are purely presentational —
// empty divs populated entirely by ui/input-otp.js from the real input's
// live value + selectionStart/selectionEnd on every
// input/selectionchange/focus/blur event. This is emphatically NOT N
// separate <input> elements with manual per-slot focus-advance-on-keypress
// JS (the common naive OTP reimplementation): native browser text editing
// — typing advances the cursor, Backspace deletes-and-moves-back, arrow
// keys move the selection — supplies focus-advance, backspace-across-
// slots, and arrow navigation entirely for free, no keyboard JS at all.
// Paste needs no special path either: it lands in the real input's value
// like any paste, and the ordinary input-event recompute (plus the
// pattern filter below) handles it exactly like typing.
//
// ADAPT (DOM-order index stamping, no `index` param — API departure from
// shadcn): shadcn's own InputOTPSlot takes an explicit `index` prop keying
// into React context's slots[index]; gsx has no equivalent shared context,
// and threading an explicit index through every call site is error-prone
// across InputOTPSeparator-split groups (a 3-group layout needs indices
// 0,1|2,3|4,5 spanning group boundaries — easy to get wrong by hand).
// InputOTPSlot below takes NO index param at all; ui/input-otp.js walks
// each root's slots in DOM order at mount and stamps data-index
// positionally — the exact same "stamp source order once, JS-computed
// identity thereafter" idiom ui/command.js already establishes for its own
// data-gsxui-index. This is a deliberate, real departure from shadcn's own
// call-site shape (ui.InputOTPSlot(), not ui.InputOTPSlot(0)), flagged
// here rather than silently ported around; see docs/jsx-parity.md.
//
// DECISION (data-gsxui-input-otp-pattern is UNANCHORED, per-character —
// NOT shadcn's whole-string-anchored REGEXP_ONLY_* constants):
// ui/input-otp.js filters keystrokes by testing ONE character at a time
// against this pattern. shadcn's own exported patterns (e.g.
// REGEXP_ONLY_DIGITS_AND_CHARS) are anchored to the WHOLE string
// (`^[0-9A-Za-z]*$`-shaped) for a different internal use inside the
// library; testing a single character against a `*`-quantified
// whole-string anchor would reject every non-empty character. Pass a bare
// per-character class instead, e.g. `[0-9]` (not `^[0-9]*$|` or
// `[0-9]*`) — see site/examples/inputotp/pattern.gsx for a worked example.
// Copying shadcn's own pattern constants verbatim here would be wrong.
//
// GAP (slots render EMPTY server-side): gsx has no client-side React
// context to pre-populate a slot's char/isActive from an initial `value`
// the way shadcn's SSR-capable React version can — every InputOTPSlot
// below renders inert (data-active="false", no text, no caret) at server-
// render time regardless of the real input's initial value, and is
// populated entirely by ui/input-otp.js after mount. A real first-paint
// divergence from shadcn, ledgered rather than silently accepted; see
// docs/jsx-parity.md.
//
// InputOTP does NOT compose ui.Input — the per-slot visual chrome
// (borders, first:/last: corner rounding, data-[active=true] ring) is
// different enough from Input's own single-box recipe that composing would
// fight rather than help (same reasoning `## sheet` gives for not
// composing DialogContent).
//
// maxlength/pattern/name/value/disabled/aria-invalid all fall through via
// attrs onto the real input, matching ui/input.gsx's own convention — no
// explicit Go params for any of them. inputmode="numeric"/
// autocomplete="one-time-code" are defaults, authored before { attrs... }
// so a caller can override them the same way ui/input.gsx's type="text" is
// overridable.
//
// Nova retarget (2026-07-24 controls source map, `## input-otp`): slot
// size-8 (was h-9 w-9), shadow-xs dropped, first:rounded-l-lg/
// last:rounded-r-lg (was -md), ring-3 (was ring-[3px], same value, v4
// token spelling only) — data-[active=true]:z-10 is KEPT regardless of
// nova's own excerpt omitting it (functionally necessary to keep the
// active ring from being occluded by a neighboring slot's border, not a
// deliberate nova drop). InputOTPGroup adopts nova's NEW group-level
// has-aria-invalid ring block (absent from new-york-v4 entirely, which
// only ever styles per-slot aria-invalid) — precedent-consistent with
// checkbox/radio/input's own aria-invalid:ring-destructive* colors, not a
// novel color introduction. InputOTPSeparator carries nova's
// [&_svg:not([class*='size-'])]:size-4 safeguard (likely a no-op since
// icon.Minus already defaults to size-4, kept regardless per the map).

//line input-otp.gsx:95:1
func InputOTP(children gsx.Node, attrs gsx.Attrs) _gsxrt.Node {
	return _gsxrt.Func(func(ctx _gsxctx.Context, _gsxw _gsxio.Writer) error {
		_gsxgw := _gsxrt.W(_gsxw)
//line input-otp.gsx:96:2
		_gsxgw.S("<div data-slot=\"input-otp\"")
		_gsxgw.BoolAttr("data-gsxui-input-otp", true)
		_gsxgw.S(" class=\"relative flex items-center gap-2 has-[input:disabled]:opacity-50\">")
//line input-otp.gsx:101:3
		_gsxgw.S("<input")
		if !attrs.Has("data-gsxui-input-otp-input") {
			_gsxgw.BoolAttr("data-gsxui-input-otp-input", true)
		}
		if !attrs.Has("inputmode") {
			_gsxgw.S(" inputmode=\"numeric\"")
		}
		if !attrs.Has("autocomplete") {
			_gsxgw.S(" autocomplete=\"one-time-code\"")
		}
		_gsxgw.S(" class=\"")
		_gsxgw.Class(_gsxcm.Merge, _gsxrt.Class("absolute inset-0 z-10 h-full w-full cursor-text opacity-0 disabled:cursor-not-allowed"), _gsxrt.Class(attrs.Class()))
		_gsxgw.S("\"")
		_gsxgw.StyleMerged("", attrs.Style())
		_gsxgw.Spread(ctx, attrs, []string{"action", "cite", "data", "formaction", "href", "manifest", "ping", "poster", "xlink:href"}, []string{"background", "src"}, []string{"imagesrcset", "srcset"}, nil, []string{"class", "style"})
		_gsxgw.S(">")
//line input-otp.gsx:108:3
		_gsxgw.Node(ctx, children)
		_gsxgw.S("</div>")
		return _gsxgw.Err()
	})
}

//line input-otp.gsx:112:1
// InputOTPGroup is an unchanged plain flex wrapper — zero behavior of its
// own, straight port — plus nova's group-level invalid-ring adoption (see
// InputOTP's own doc comment).

//line input-otp.gsx:115:1
func InputOTPGroup(children gsx.Node, attrs gsx.Attrs) _gsxrt.Node {
	return _gsxrt.Func(func(ctx _gsxctx.Context, _gsxw _gsxio.Writer) error {
		_gsxgw := _gsxrt.W(_gsxw)
//line input-otp.gsx:116:2
		_gsxgw.S("<div")
		if !attrs.Has("data-slot") {
			_gsxgw.S(" data-slot=\"input-otp-group\"")
		}
		_gsxgw.S(" class=\"")
		_gsxgw.Class(_gsxcm.Merge, _gsxrt.Class("flex items-center has-aria-invalid:ring-destructive/20 dark:has-aria-invalid:ring-destructive/40 has-aria-invalid:border-destructive rounded-lg has-aria-invalid:ring-3"), _gsxrt.Class(attrs.Class()))
		_gsxgw.S("\"")
		_gsxgw.StyleMerged("", attrs.Style())
		_gsxgw.Spread(ctx, attrs, []string{"action", "cite", "data", "formaction", "href", "manifest", "ping", "poster", "src", "xlink:href"}, []string{"background"}, []string{"imagesrcset", "srcset"}, nil, []string{"class", "style"})
		_gsxgw.S(">")
//line input-otp.gsx:121:3
		_gsxgw.Node(ctx, children)
		_gsxgw.S("</div>")
		return _gsxgw.Err()
	})
}

//line input-otp.gsx:125:1
// InputOTPSlot renders EMPTY at server-render time (see InputOTP's own GAP
// doc comment) — no index param (see the ADAPT doc comment above),
// no children: ui/input-otp.js owns this element's entire text/caret
// content after mount.

//line input-otp.gsx:129:1
func InputOTPSlot(attrs gsx.Attrs) _gsxrt.Node {
	return _gsxrt.Func(func(ctx _gsxctx.Context, _gsxw _gsxio.Writer) error {
		_gsxgw := _gsxrt.W(_gsxw)
//line input-otp.gsx:130:2
		_gsxgw.S("<div")
		if !attrs.Has("data-slot") {
			_gsxgw.S(" data-slot=\"input-otp-slot\"")
		}
		if !attrs.Has("data-active") {
			_gsxgw.S(" data-active=\"false\"")
		}
		_gsxgw.S(" class=\"")
		_gsxgw.Class(_gsxcm.Merge, _gsxrt.Class("relative flex size-8 items-center justify-center border-y border-r border-input text-sm transition-all outline-none first:rounded-l-lg first:border-l last:rounded-r-lg aria-invalid:border-destructive data-[active=true]:z-10 data-[active=true]:border-ring data-[active=true]:ring-3 data-[active=true]:ring-ring/50 data-[active=true]:aria-invalid:border-destructive data-[active=true]:aria-invalid:ring-destructive/20 dark:bg-input/30 dark:data-[active=true]:aria-invalid:ring-destructive/40"), _gsxrt.Class(attrs.Class()))
		_gsxgw.S("\"")
		_gsxgw.StyleMerged("", attrs.Style())
		_gsxgw.Spread(ctx, attrs, []string{"action", "cite", "data", "formaction", "href", "manifest", "ping", "poster", "src", "xlink:href"}, []string{"background"}, []string{"imagesrcset", "srcset"}, nil, []string{"class", "style"})
		_gsxgw.S("></div>")
		return _gsxgw.Err()
	})
}

//line input-otp.gsx:138:1
// InputOTPSeparator: icon.Minus, static, unchanged from shadcn.

//line input-otp.gsx:139:1
func InputOTPSeparator(attrs gsx.Attrs) _gsxrt.Node {
	return _gsxrt.Func(func(ctx _gsxctx.Context, _gsxw _gsxio.Writer) error {
		_gsxgw := _gsxrt.W(_gsxw)
//line input-otp.gsx:140:2
		_gsxgw.S("<div")
		if !attrs.Has("data-slot") {
			_gsxgw.S(" data-slot=\"input-otp-separator\"")
		}
		if !attrs.Has("role") {
			_gsxgw.S(" role=\"separator\"")
		}
		_gsxgw.S(" class=\"")
		_gsxgw.Class(_gsxcm.Merge, _gsxrt.Class("[&_svg:not([class*='size-'])]:size-4"), _gsxrt.Class(attrs.Class()))
		_gsxgw.S("\"")
		_gsxgw.StyleMerged("", attrs.Style())
		_gsxgw.Spread(ctx, attrs, []string{"action", "cite", "data", "formaction", "href", "manifest", "ping", "poster", "src", "xlink:href"}, []string{"background"}, []string{"imagesrcset", "srcset"}, nil, []string{"class", "style"})
		_gsxgw.S(">")
//line input-otp.gsx:141:3
		_gsxgw.Node(ctx, icon.Minus())
		_gsxgw.S("</div>")
		return _gsxgw.Err()
	})
}
